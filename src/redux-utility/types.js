// @flow

// export type PlainAction = {| type: string, payload?: any, meta?: any |};

// export type Reducer = <S, A, D>(s: S, a: A, d?: D) => S;

// export type RootState = {};

// export type MakeReducer = {|
//     reducer: Reducer,
//     getDependencies?: (r: RootState) => $Shape<RootState> 
// |}

// declare function createReducer(a: MakeReducer): MakeReducer;